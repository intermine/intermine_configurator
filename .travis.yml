language: generic
python: "3.6"
jdk: "openjdk8"

services:
  - redis-server

stages:
  - tests
  - name: apiMerge
    if: branch == master

jobs:
  include:
    - stage: tests
      script: ./gradlew test
    - stage: apiMerge
      install: "pip install -r requirements.txt"
      script:
        - 'if [ "$TRAVIS_PULL_REQUEST" = "false" ]; then python autoMerger.py ; fi'
